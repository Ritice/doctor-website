version: 0.2

phasesDD:
  install:
    commands:
      - echo "Installing dependencies..."
  pre_build:
    commands:
      - echo "Start prebuild and end it"
  build:
    commands:
      - echo "Running build"
  post_build:
    commandsDDD:
      - echo "Build completed successfully."

artifacts:
  files:
    - "**/*"
  base-directory: .
